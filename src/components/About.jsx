import React from "react";
import Tilt from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className='bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt='web-development'
          className='w-16 h-16 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]'
        style={{textAlign : "justify"}}
      >
       Since 2017, we have been dedicated to transforming advanced and complex ideas into tangible AI and machine learning products.<br/> Engaging with clients from the USA, Brazil, Israel, the UK, and Australia, we focus on realizing the visions of founders and startups by adopting the latest trends, tools, and frameworks in the AI and ML domains. Our mission is to make AI affordable, guided by our core values of originality and clarity. 
       <br/> Based in Haridwar, Uttarakhand, India, our team of 11-20 employees—from India specializes in domains such as LLMs, HuggingFace, AWS, GCP, SQL, Automation, Computer Vision, Lidars, Python, JavaScript, React, Swift, and SaaS. We pride ourselves on delivering products that meet international standards and truly embody the innovative spirit of our clients.
      </motion.p>

      <div className='mt-20 flex flex-wrap gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
